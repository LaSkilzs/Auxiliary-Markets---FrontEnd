const Exchange = artifacts.require("Exchange");

contract("Exchange", () => {
  // const exchange = await Exchange.deployed();

  it("should create a on the exchange token", async () => {});
  it("should add a token to the exchange", async () => {});
  it("should update total token supply amount", async () => {});
  it("it should returrn true when exchange hasToken", async () => {});
  it("it should returrn false  when exchange does not hasToken", async () => {});
});
