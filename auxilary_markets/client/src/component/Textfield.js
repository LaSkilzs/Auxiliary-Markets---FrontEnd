import React from "react";
import TextField from "@material-ui/core/TextField";

const TradingTextfield = props => {
  return (
    <React.Fragment>
      <TextField
        // className={classes.textField}
        // value={values.name}
        // onChange={handleChange("name")}
        margin="normal"
      />
    </React.Fragment>
  );
};

export default TradingTextfield;
